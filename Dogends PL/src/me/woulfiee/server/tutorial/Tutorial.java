/*******************************************************************************
 * Copyright (c) 31.8.2016 by Woulfiee
 ******************************************************************************/

package me.woulfiee.server.tutorial;

import org.bukkit.event.Listener;

/**
 * 
 * @author Woulfiee
 *
 */
public class Tutorial implements Listener {

	// private List<Player> d = new ArrayList<Player>();
	// public static Location loc;
	//
	// @EventHandler
	// public void onSignChange(SignChangeEvent e) {
	//
	// }
	//
	// @EventHandler
	// public void onSignClick(final SignChangeEvent e) {
	// if ) {
	// if (!d.contains(e.getPlayer())) {
	// d.add(e.getPlayer());
    // e.getPlayer().sendMessage("§a§lINTRODUCTION> §eHi,");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
    // 1.0F, 1.0F);
	//
	// Bukkit.getScheduler().runTaskAsynchronously(Dogends.plugin, new
	// Runnable() {
	// @Override
	// public void run() {
	// try {
	//
	// Thread.sleep(5000);
	//
	// e.getPlayer()
    // .sendMessage("§a§lINTRODUCTION> §eWe will tell you now what to do on the
    // server.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(10000);
	//
    // e.getPlayer().sendMessage("§a§lFIRST STEPS> §eThis is survival + factions
    // server.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lFIRST STEPS> §eOn the spawn there is a rule board. Please read it
    // first.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lFIRST STEPS> §eIf you finish, you can go through the portal, which
    // is located on the spawn.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
    // e.getPlayer().sendMessage("§a§lFIRST STEPS> §eThere are tokens and
    // levels.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lFIRST STEPS> §eToken is a currency on this server. You get it by
    // killing people. You get 250 tokens when you kill someone and you lose 500
    // tokens when someone kills you.");
	// e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lFIRST STEPS> §eYou can earn levels by killing people. If you kill
    // someone, you will get 1 level up, but when someone kills you, you get 2
    // levels down.");
	// e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(10000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lFIRST STEPS> §eIf you'd go through the portal, you will be
    // teleported to the survival spawn. Your adventure starts here.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(10000);
	//
    // e.getPlayer().sendMessage("§a§lSURVIVAL> §eYou must collect many
    // items.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
    // e.getPlayer().sendMessage("§a§lSURVIVAL> §eBeware of other players who
    // can kill you.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(10000);
	//
    // e.getPlayer().sendMessage("§a§lFACTIONS> §eTo create a faction, use §a/f
    // create <NAME>§e.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
    // 1.0F, 1.0F);
	//
	// Thread.sleep(5000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lFACTIONS> §eIf you will claim land, you can build a house, place a
    // chest in there and store your items safer.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(10000);
	//
    // e.getPlayer().sendMessage("§a§lEND> §eI think, that this is the end.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
    // 1.0F, 1.0F);
	//
	// Thread.sleep(3000);
	//
	// e.getPlayer().sendMessage(
    // "§a§lEND> §eFor help do /helpop if there will be any staff member
    // online.");
    // e.getPlayer().playSound(e.getPlayer().getLocation(), Sound.NOTE_PIANO,
	// 1.0F, 1.0F);
	//
	// Thread.sleep(3600000);
	// d.remove(e.getPlayer());
	// } catch (Exception ignored) {
	// }
	// }
	// });
	// }
	// e.setCancelled(true);
	// }
	// }

}
